<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <meta charset="utf-8">
        <title>TEST</title>
    </head>

            <% taches.forEach(function(tache){ %>
                <li><%= tache.objectif %></li>
                <li><%= tache.date %></li>
                <% }); %>
        </ul>
        </ul>
    </body>
</html>
